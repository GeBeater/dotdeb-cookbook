# The metadata source is like saying: “There is a metadata.rb file within the same
# relative path of my Berksfile” This allows you to resolve a Cookbook’s dependencies
# that you are currently working on.
metadata

cookbook 'apt', :git => 'https://github.com/opscode-cookbooks/apt.git', :branch => 'v2.3.4'